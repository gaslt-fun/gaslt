{
  "name": "@gaslt/sdk",
  "version": "0.4.2",
  "description": "TypeScript client for the sospeso gas-abstraction protocol: registry queries, sponsorship matching, and relayer requests.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gaslt-fun/gaslt.git",
    "directory": "sdk"
  },
  "homepage": "https://gaslt.fun",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["solana", "gas", "sospeso", "relayer", "sdk"],
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.0"
  }
}
